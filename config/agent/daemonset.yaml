apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: netprobe-agent
  namespace: netprobe-system
  labels:
    app: netprobe-agent
spec:
  selector:
    matchLabels:
      app: netprobe-agent
  template:
    metadata:
      labels:
        app: netprobe-agent
    spec:
      hostNetwork: false
      containers:
      - name: agent
        image: selfheal-agent:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
          name: control
        - containerPort: 9376
          name: data
        env:
        - name: NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
        resources:
          limits:
            cpu: 100m
            memory: 64Mi
          requests:
            cpu: 50m
            memory: 32Mi
        securityContext:
          capabilities:
            drop:
            - NET_RAW
