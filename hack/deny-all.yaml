# Deny-all NetworkPolicy for fault scenario F6.
# Blocks all ingress and egress traffic in netprobe-system.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: deny-all-netprobe
  namespace: netprobe-system
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
